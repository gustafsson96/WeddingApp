@model WeddingApp.Models.Wedding


@if (!string.IsNullOrEmpty(Model.HeaderImageUrl))
{
    <img src="@Model.HeaderImageUrl" alt="Wedding Header" class="img-fluid" />
}
<div>
    <h1>@Model.FirstPerson & @Model.SecondPerson</h1>
</div>

<section>
    <h2>Welcome!</h2>
    <p>@Model.Date.ToShortDateString() kl @Model.Time</p>
    <p>@Model.Venue, @Model.City</p>
</section>

@if (!string.IsNullOrEmpty(Model.AdditionalInfo))
{
    <section>
        <h2>Information</h2>
        <p>@Model.AdditionalInfo</p>
    </section>
}