@section Styles {
    <link rel="stylesheet" href="~/css/dashboard.css" asp-append-version="true" />
}

@{
    ViewData["Title"] = "Dashboard";
}

@model WeddingApp.Models.Wedding

<h1>Admin Dashboard</h1>

@if (Model == null)
{
    <div class="dashboard-card">
        <h3>No wedding created yet</h3>
        <a asp-controller="Weddings" asp-action="Create" class="btn-primary">
            Create Wedding
        </a>
    </div>
}
else
{
    <div class="dashboard-grid">

        <div class="dashboard-card">
            <h3>Your Wedding</h3>
            <p>@Model.FirstPerson & @Model.SecondPerson</p>
            <p>@Model.Date.ToShortDateString()</p>

            <a asp-controller="Weddings" asp-action="Edit" asp-route-id="@Model.WeddingId">
                Edit Wedding
            </a>
            <br />
            <a href="/wedding/@Model.PublicSlug" target="_blank">
                View Public Page
            </a>
        </div>

        <div class="dashboard-card">
            <h3>Guests</h3>
            <a asp-controller="Guests" asp-action="Index">
                Manage Guests
            </a>
            <br />
            <a asp-controller="Guests" asp-action="Create">
                Add Guest
            </a>
        </div>

        <div class="dashboard-card">
            <h3>Wishlist</h3>
            <a asp-controller="Gifts" asp-action="Index">
                Manage Gifts
            </a>
            <br />
            <a asp-controller="Gifts" asp-action="Create">
                Add Gift
            </a>
        </div>

    </div>
}